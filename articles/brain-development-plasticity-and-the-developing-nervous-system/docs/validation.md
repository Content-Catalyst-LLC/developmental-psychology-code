# Validation

Suggested checks:

```bash
make python
make scenarios
make rust
make go
```

Inspect row counts, unique child counts, unique context counts, missing values, plausible ranges, profile distributions, support/stress gradients, environmental-risk gradients, and context-level neurodevelopmental support summaries.
