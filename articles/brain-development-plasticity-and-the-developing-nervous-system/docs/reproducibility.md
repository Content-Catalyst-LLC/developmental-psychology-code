# Reproducibility

Run:

```bash
make python
make scenarios
```

Optional:

```bash
make r
make c
make cpp
make go
make rust
make julia
make fortran
```

Expected outputs include synthetic panel data, model summaries, trajectory CSVs, scenario comparison CSVs, and PNG figures in `outputs/`.
