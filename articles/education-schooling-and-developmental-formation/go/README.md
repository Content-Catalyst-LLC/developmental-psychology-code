# Go

The Go utility reads the generated CSV and prints core means for development, connectedness, teacher support, peer belonging, and school stress.

Run:

```bash
go run go/schooling_summary.go data/schooling_development_panel.csv
```
