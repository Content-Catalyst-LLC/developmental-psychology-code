# Go

The Go utility reads the generated CSV and prints core means for development, gains, losses, SOC, and contextual support.

Run:

```bash
go run go/lifespan_baltes_summary.go data/lifespan_baltes_panel.csv
```
