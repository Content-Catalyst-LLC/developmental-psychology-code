# Go

The Go utility reads the generated CSV and prints core means for development, embedded stress, embedded support, care, and current stress.

Run:

```bash
go run go/plasticity_summary.go data/genes_environment_plasticity_panel.csv
```
