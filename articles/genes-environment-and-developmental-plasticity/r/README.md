# R Workflow

This folder provides mixed-effects models for the synthetic genes-environment-plasticity panel.

Run:

```bash
Rscript r/model_genes_environment_plasticity.R
```

The script estimates development and embedding models with children nested within contexts.
