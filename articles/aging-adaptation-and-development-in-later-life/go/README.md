# Go

The Go utility reads the generated CSV and prints core means for adjustment, functional fit, support, health burden, and adaptive strategy.

Run:

```bash
go run go/aging_adaptation_summary.go data/aging_adaptation_later_life_panel.csv
```
