# Go

The Go utility reads the generated CSV and prints core means for development, participation, access, and barrier burden.

Run:

```bash
go run go/accessibility_summary.go data/disability_neurodivergence_panel.csv
```
