# Go

The Go utility reads the generated CSV and prints core means for development, family support, parenting, family climate, and stress.

Run:

```bash
go run go/family_systems_summary.go data/family_systems_panel.csv
```
