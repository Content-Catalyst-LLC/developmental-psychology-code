# Data

This folder contains synthetic timing-window data and metadata.

Generate the main dataset with:

```bash
python3 ../python/generate_timing_panel.py
```

The generated dataset is synthetic and contains no real personal information.

